name: Apply gesture navigation and message fold

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-pager-master-detail.yml'
      - '.github/scripts/apply_pager_master_detail.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply source migration
        run: python3 .github/scripts/apply_pager_master_detail.py

      - name: Verify patch formatting
        run: git diff --check

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4

      - name: Compile Kotlin
        run: gradle :app:compileDebugKotlin --stacktrace --no-daemon

      - name: Commit validated source changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/example/ui/screens/FeedScreen.kt \
                  app/src/main/java/com/example/ui/screens/MessagesScreen.kt \
                  app/src/main/java/com/example/MainActivity.kt
          if git diff --cached --quiet; then
            echo "Source already up to date."
            exit 0
          fi
          git commit -m "feat: add swipe families and folding message pane"
          git pull --rebase origin main
          git push origin HEAD:main
