name: Apply feed game icon and author identity fix

on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/apply_feed_game_icon_author_identity_fix.py'
      - '.github/workflows/apply-feed-game-icon-author-identity-fix.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Apply feed navigation and author identity fixes
        run: python3 .github/scripts/apply_feed_game_icon_author_identity_fix.py

      - name: Compile Kotlin
        run: gradle :app:compileDebugKotlin --stacktrace --no-daemon

      - name: Commit source fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/example/ui/screens/FeedScreen.kt \
                  app/src/main/java/com/example/ui/components/PremiumFeedChrome.kt \
                  app/src/main/java/com/example/data/supabase/SupabaseService.kt
          if git diff --cached --quiet; then
            echo "No source changes needed."
            exit 0
          fi
          git commit -m "fix: make Reel and Game feed icons and restore author identity"
          git push origin main
