# Blinkng

Student Spotlight — Android campus social application.

The main app is under `app/` and uses Kotlin/Jetpack Compose with Supabase for authentication, data, storage, and realtime features.

## Verification

Runtime fixes and Android build checks run automatically on pushes to `main`.
